/**
 * @comment public class Queue 
 * 队列也是表。然而，使用队列时插入在一端进行而删除则是在另一端进行。
 * 队列的基本操作是Enqueue（入队），它是在表的末端（也叫队尾）插入一个元素，还有Dequeue（出队）,它是删除在表的开头的元素。
 */

package crawlingtosea.algorithms.adt
{
	
	/**
	 * @Queue 
	 * @author mengtianwxs
	 * @createTime 2014-3-13
	 */	
	
	public class Queue
	{
		public function Queue()
		{
		}
	}
}